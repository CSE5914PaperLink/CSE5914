# Firebase App Hosting configuration for Next.js frontend

# Settings for Cloud Run
runConfig:
  minInstances: 0
  maxInstances: 10
  cpu: 1
  memoryMiB: 512
  concurrency: 80

# (Optional) Only if you REALLY need custom commands.
# For a standard Next.js app, you can omit this entirely and let App Hosting
# infer `npm run build` and the correct run command.
# scripts:
#   buildCommand: npm run build
#   runCommand: npm run start

# Environment variables and secrets
env:
  # Server-side environment variables
  - variable: BACKEND_URL
    secret: backend-url-secret
    availability:
      - BUILD
      - RUNTIME

  # Client-side environment variables
  - variable: NEXT_PUBLIC_BACKEND_URL
    secret: backend-url-secret
    availability:
      - BUILD
      - RUNTIME

  # Firebase configuration (client-side)
  - variable: NEXT_PUBLIC_FIREBASE_API_KEY
    secret: firebase-api-key-secret
    availability:
      - BUILD
      - RUNTIME

  - variable: NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN
    secret: firebase-auth-domain-secret
    availability:
      - BUILD
      - RUNTIME

  - variable: NEXT_PUBLIC_FIREBASE_PROJECT_ID
    secret: firebase-project-id-secret
    availability:
      - BUILD
      - RUNTIME

  - variable: NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET
    secret: firebase-storage-bucket-secret
    availability:
      - BUILD
      - RUNTIME

  - variable: NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID
    secret: firebase-messaging-sender-id-secret
    availability:
      - BUILD
      - RUNTIME

  - variable: NEXT_PUBLIC_FIREBASE_APP_ID
    secret: firebase-app-id-secret
    availability:
      - BUILD
      - RUNTIME
